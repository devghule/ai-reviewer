export const REVIEW_RETENTION_DAYS = 14

export const DEFAULT_TIMEOUT_MS = 30000

export const STORAGE_PATHS = {
  REVIEWS: 'storage/reviews',
  CONTEXTS: 'storage/contexts',
  CACHE: 'storage/cache',
} as const