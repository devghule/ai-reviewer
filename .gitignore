# Next.js
.next/
out/

# Dependencies
node_modules/

# Environment — contains API keys, never commit
.env.local
.env*.local

# Repo config — contains local machine paths, never commit
config/repos.json
config/selected-model.json

# Reviews — temporary local storage
reviews/

# OS
.DS_Store
Thumbs.db
